const filesystem = require('fs');
const JSON = require('JSON');
import character from './character.js';

//Is using names the best way to identify the character? not sure. needs more planning
let char = new character('Sam Ple');

